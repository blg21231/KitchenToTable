#pull all dishes from csv
#show to user

order = []

#pull user data from user_id
#order.append(user data)

#get user seletions
for dish in userselection:
#order.append(selection)

#look up dishes' prices 
total = 0
for dish in dishes:
  total += dishes.loc[dishes.name == dish.name].price
 
 #charge user total
 
 #User review
 #if user chooses dish:
 #dishes.stars.append(user input)
 #dishes.reviews.append(user input)
 
  #if user chooses chef:
 #chefs.stars.append(user input)
 #chefs.reviews.append(user input)
 
  #if user chooses waiter:
 #waiters.stars.append(user input)
 #waiters.reviews.append(user input)
