chefs[chefs.user_id == user.id].pfp = user upload
chefs[chefs.user_id == user.id].banner = user upload
chefs[chefs.user_id == user.id].dishes = user upload
 
 #User review
 #if user chooses waiter:
 #waiters.stars.append(user input)
 #waiters.reviews.append(user input)
